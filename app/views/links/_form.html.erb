<%= form_for([@article, @article.links.build]) do |f| %>
  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </p>
  <p>
    <%= f.label :href %><br>
    <%= f.text_area :href %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>
